# TOOLS

## remember
Save important information in structured memory.

## recall
Search memory and return related information (QMD enabled only).

## skill generation (proposal)
Enabled in `learning`/`full` modes. Writing files is on by default; set `COGITO_ALLOW_SKILL_WRITE=0` to disable.
Generated skills are automatically reviewed and loaded when they pass review.
